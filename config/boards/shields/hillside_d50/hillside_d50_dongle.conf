CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2
CONFIG_BT_MAX_CONN=7

CONFIG_ZMK_SLEEP=n

CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=6
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=3

CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_DONGLE_SCREEN_FLIPPED=y
CONFIG_DONGLE_SCREEN_DEFAULT_BRIGHTNESS=50
CONFIG_DONGLE_SCREEN_SYSTEM_ICON=0

# Keep the dongle screen on while the keyboard is active. ZMK's idle
# timer defaults to 30s and triggers display shutoff; bump to ~24h so
# typing always wakes/keeps the screen but it still idles overnight.
CONFIG_ZMK_IDLE_TIMEOUT=86400000
