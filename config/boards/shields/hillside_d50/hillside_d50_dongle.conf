CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2
CONFIG_BT_MAX_CONN=7

CONFIG_ZMK_SLEEP=n

CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=6
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=3

CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_PROSPECTOR_ROTATE_DISPLAY_180=y
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=50
CONFIG_PROSPECTOR_MODIFIER_OS_MAC=y

# Override PM_SOFT_OFF=y from hillside_d50.conf — soft-off is a battery
# feature for the halves, not the USB-powered dongle. Leaving it on
# pulls in PM_DEVICE + ZMK_PM_DEVICE_SUSPEND_RESUME, which can suspend
# the ST7789v panel on idle without a resume path.
CONFIG_ZMK_PM_SOFT_OFF=n
CONFIG_PM_DEVICE=n
